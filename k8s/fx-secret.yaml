apiVersion: v1
kind: Secret
metadata:
  name: fx-secret
  namespace: quetzalship
type: Opaque
stringData:
  # API Keys (if needed, base64 encoded in real scenarios)
  FX_PRIMARY_API_KEY: ""
  FX_SECONDARY_API_KEY: ""
  # Redis password (if needed)
  REDIS_PASSWORD: ""
